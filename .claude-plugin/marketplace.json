{
  "$schema": "https://json.schemastore.org/claude-code-plugin-marketplace.json",
  "name": "prompt-lib",
  "owner": {
    "name": "{{GIT_USER_NAME}}",
    "email": "{{LOGGED_IN_EMAIL}}"
  },
  "description": "Personal Claude Code library — skills, agents, hooks, MCP servers, and output styles.",
  "plugins": [
    {
      "name": "prompt-lib",
      "source": "./global",
      "description": "Skills, agents, hooks, MCP servers, and output styles for Claude Code.",
      "author": {
        "name": "{{GIT_USER_NAME}}",
        "email": "{{LOGGED_IN_EMAIL}}"
      },
      "homepage": "{{REPO_URL}}",
      "repository": "{{REPO_URL}}",
      "keywords": ["skills", "agents", "hooks", "mcp", "claude-code"],
      "category": "productivity"
    }
  ]
}
