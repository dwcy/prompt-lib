#!/usr/bin/env sh
# Start the Cabal Tauri desktop development shell.

set -u

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$SCRIPT_DIR/apps/cabal-desktop" || exit 1
exec pnpm tauri dev "$@"
