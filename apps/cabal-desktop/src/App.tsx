// Placeholder shell — replaced by the real workspace shell in T023.
const APP_NAME = "Cabal";
const BACKEND_STATUS = "backend: disconnected";

export default function App() {
  return (
    <main>
      <h1>{APP_NAME}</h1>
      <p>{BACKEND_STATUS}</p>
    </main>
  );
}
