{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main Cabal window, plus execute permission for the dev-mode backend launch (`uv run cabal-backend`). The production `cabal-backend` sidecar permission is added in T096 alongside its `bundle.externalBin` entry, once the PyInstaller binary exists.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "uv",
          "cmd": "uv",
          "args": ["run", "cabal-backend"]
        }
      ]
    }
  ]
}
