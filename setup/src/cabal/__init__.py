"""cabal — Claude Code Setup Wizard.

A Textual TUI for deploying the prompt-lib `global/` tree into `~/.claude/`,
managing MCP servers via `claude mcp`, scaffolding new projects, and running
drift / restore operations against the local Claude Code config.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
